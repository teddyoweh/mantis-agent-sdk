You are working autonomously in /Users/teddy/Documents/code/any-agent-sdk, the Mantis Agent SDK. The user explicitly authorized --dangerously-skip-permissions. Do not ask questions. Inspect before editing, preserve all existing user changes, and never reset/checkout/revert/delete unrelated work. Do not commit or push.

MISSION
Turn the existing workflow/background-job/subagent foundation into a coherent, expert-grade Claude Code-inspired orchestration product extension. Do not replace the architecture; build on mantis_agent/workflow.py, jobs.py, subagent.py, coordinator.py, workflow_view.py, and the TUI. Make the implementation real, integrated, tested, documented, and pleasant to use.

REFERENCE MATERIAL
We extracted the shipped Claude Code 2.1.220 workflow instructions at:
/Users/teddy/Documents/code/claude-code-2.1.220-source/workflow-excerpts.txt
Read it for product behavior and concepts, but do not copy proprietary code verbatim. Reimplement concepts idiomatically for this Python project.

CURRENT REPO FACTS
- Workflow engine already supports phases, agent(), parallel(), pipeline(), state/events, stop, pause/resume, cancel, skip, retry, and save.
- JobManager supports detached subagent work in memory.
- Subagents have built-in explore/plan/general-purpose/verify and custom Markdown personas.
- TUI already has /workflows, /jobs, /job, /agents and drill-down, but orchestration is fragmented.
- There are many existing uncommitted user changes. Preserve them.

PRODUCT GOAL
Create a reusable orchestration base that makes users experts in ten concrete capabilities:
1. Declarative named workflows.
2. Multi-phase workflow execution.
3. Parallel fan-out and per-item pipelines.
4. Background workflow jobs that return immediately and can be monitored.
5. Disposable expert subagents and custom agent types.
6. Live workflow viewing UI with status, phase rail, activity, token/cost/timing data.
7. Enter/drill into a subagent to see its prompt, activities, output and error.
8. View and navigate multiple workflows in one UI.
9. Control workflows and agents: stop, pause/resume, cancel, skip, retry, save/export.
10. Resume/replay and inspect durable workflow artifacts/history after completion.

IMPLEMENTATION REQUIREMENTS
A. First map the exact current architecture and tests. Then implement the smallest coherent extension that satisfies the goal.
B. Introduce a first-class workflow definition/template layer. Prefer Markdown or Python/JSON only if it fits project conventions. Support project and user locations, with clear precedence, discovery, validation, and named invocation. Built-in reusable templates should demonstrate understand, design, review/research, and implementation/verification patterns without overengineering.
C. Provide a model-facing workflow tool (or equivalent integrated API) with explicit opt-in semantics. It must be safe and clear about potentially high token use. It should support named workflows and declarative structured input rather than arbitrary unsafe eval. If dynamic scripting is inappropriate in Python, design an idiomatic declarative DAG/stage format.
D. Background the entire workflow through JobManager so invocation can return a job/run ID immediately. Job and workflow identity should be connected. The user must be able to monitor from /jobs and /workflows.
E. Make workflow runs durable enough to inspect completed history and resume/replay where technically sound. Use the existing save/state mechanisms; avoid inventing a database. Store under MANTIS_AGENT_HOME or project .mantis using established conventions. Never persist secrets.
F. Upgrade /workflows into a genuinely useful multi-workflow viewer. Preserve terminal UI conventions. Expose the existing control-plane operations that are currently hidden: resume, cancel/skip/retry selected agent where valid, plus stop, pause, inspect, save. Make controls discoverable in the footer/help.
G. Upgrade subagent drill-down to include prompt (possibly truncated), full recent activities, model/type/status, timing, usage/cost, result/error, and a clear relationship to workflow/job. Do not expose hidden model chain-of-thought.
H. Connect normal background subagents/jobs and workflow agents conceptually in UI without breaking existing /jobs behavior.
I. Add a concise expert guide and examples to README/docs covering the ten capabilities, lifecycle, controls, custom agents, saved workflows, and cost/safety behavior.
J. Add deterministic tests. No real model/network calls. Cover definition loading/precedence/validation, named execution, background registration, persistence/history, controls, and view formatting where relevant.
K. Keep public API/SemVer policy in mind. Export only intentionally stable types. Avoid broad unrelated refactors.
L. Run focused tests, formatting/lint for changed files, then the broad relevant suite. Fix failures caused by your work. Report pre-existing unrelated failures separately.

UX PRINCIPLES
- One mental model: workflow > phase > agent; workflow may also be a background job.
- Every long operation has an ID and observable status.
- Multiple workflows are normal, not an edge case.
- Enter means inspect/drill down; Escape means back.
- Make empty states teach the command needed to start a workflow.
- Every action should explain eligibility and failure without crashing.
- Show enough activity to establish trust, but never hidden reasoning.

BOUNDARIES
- Do not build a separate React dashboard unless the existing architecture clearly requires it. TUI-first is preferred.
- Do not copy minified Claude source or prompts verbatim.
- Do not use shell-based destructive git commands.
- Do not modify generated lockfiles unless dependency changes are essential (prefer no new dependency).
- Do not stop at a plan. Implement and verify.

Start by reading the relevant files and git diff. Then execute the implementation autonomously. At the end, print: changed files, implemented capabilities, exact tests run/results, and remaining limitations.